/* ReactJS Fetching Data from Remote URL. */
/* How to fetch data in Higher-Order Components. */

// If you need to pass more query parameters to your higher-order component later on, you always can extend the arguments
// in the function signature.

const withFetching = (url, query) => (Comp) =>

//  ...
